---
description: Build the ASP.NET Core backend projects
agent: dotnet-dev
---

Build all backend projects from the solution root.

Run `dotnet build` and report any errors or warnings.

If there are build errors:
1. Fix the errors with minimal changes
2. Rebuild to verify
3. Report what was fixed
