---
description: Build the Vue 3 frontend
agent: vue-dev
---

Build the Vue 3 frontend project.

Run from `src/OpenPeer.Web/`:
1. `npm install` (if needed)
2. `npm run build`
3. Report any errors or warnings

If there are build errors, fix them with minimal changes and rebuild to verify.
