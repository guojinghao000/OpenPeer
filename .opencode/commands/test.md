---
description: Run backend unit tests
agent: dotnet-dev
---

Run all backend tests from the solution root.

Execute `dotnet test` and report:
- Total tests passed/failed/skipped
- Any failing test names and error messages
- Summary of test coverage if available

If tests fail, analyze the failures and suggest fixes.
